- Question 1: In naïve Bayesian text classification, we assume that each document is generated by a multinomial distribution. What are the parameters of the multinomial distribution in the context of text documents?
	- We need $Pr(w | c)$ where $w$ is a word, $c$ is a class, and the length of the text document $d$. ![[Pasted image 20260306193318.png]]
- Question 2: [[Precision]] and [[recall]] usually have the following relationship:
	- If precision is high, then recall is usually low. We use [[F-Score]] to check when both are high.
- Question 3: In naïve Bayesian classification, which of the following statements is true?
	- Naive Bayesian classification assumes [[conditional independence]]!
- Question 4: In Naive Bayesian classification, which is assumed?
	- $Pr(A_1 = a_1 | A_2=a_2, A_3=a_3, C = c_i) == Pr(A_1=a_1 | C = c_i)$
	- Remember that NB assumes [[conditional independence]], meaning that each variable is independent given a class C
- Question 5: In the linear separable [[Support Vector Machine]], which of the following is the complementarity condition?
	- $\alpha_i(y_i(\langle x_i * w \rangle+b)-1)=0, i=1,2,...,r$
- Question 6: Which of the following is true about the decision tree algorithm?
	- Decision tree algorithm is a divide-and-conquer algorithm
- Question 7: In naïve Bayesian text classification, a classifier needs the following probabilities (w is a word, _c_ is a class and _d_ is a document)
	- $Pr(w | c) \text{ and } Pr(c)$
- Question 8: In Naïve Bayesian text classification, we use smoothing to deal with
	- Words that appear in testing but do not appear in training.
	- We use smoothing to ensure that the probability is not 0!
- Question 9: What does a [[Kernel function]] in [[Support Vector Machine]]s compute?
	- The dot product of 2 vectors in some feature space computed using input vectors.
- Question 10: In linearly separable SVM, we say that the complementarity condition indicates
	- That the data points on the margin hyperplanes are support vectors
- Question 11: In linearly non-separable SVM
	- The data points on the margin hyperplanes are support vectors
	- Noisy data points are support vectors
	- data points between the margin hyperplanes are support vectors
- Question 12: What does the inequality $y_i(\langle w*x_i\rangle +b) \ge 1, i=1,2,...,n$ mean in SVM?
	- All positive data points must be on the positive side, and all negative data points must be on the negative side
- Question 13: What is the neural network training algorithm called?
	- [[Backpropagation]]
- Question 14: What is the method used by linear regression to build a model?
	- [[gradient descent]]
- Question 15: Which of the following classification methods can be used for discretization?
	- [[decision tree]]
	- Naive bayes is text probability
	- SVM used for positive and negative classes
- Question 16: A neuron computes
	- The sum of the inputs and an activation
- Question 17: Linear regression uses:
	- A continuous input and output variable
- Question 18: We have a neural network with 3 layers. The input layer has 4 neurons, the output layer has 2 neurons, and the hidden layer has 4 neurons. How many parameters does the network have?
	- 30 total parameters
	- 4 inputs, (4 * 4) for first layer to second, (4 * 2) for second to third, (2 * 1) for output = $4 + (4*4) + (4*2) + (2*1) = 30$
- Question 19: Which of the ensemble methods votes with unequal weights for the resulting classifiers?
	- Neither random forest nor bagging use votes with unequal weights
	- Boosting uses votes with unequal weights
- Question 20: Which of the ensemble algorithms weighs each training example
	- Boosting weighs each training example
- Question 21: Which supervised learning method is based on binary classification?
	- SVM - It uses positive and negative classes -> 2 / binary class
- Question 22: Which of the following is true about [[kNN]]?
	- KNN does not build a model
- Question 23: Does the decision true have an overfitting problem and how is it solved?
	- Yes: Usually solved through post-pruning
- Question 24: What's the complexity of the decision tree algorithm w/ discrete and continuous attributes?
	- $O(n*log(n))$
- Question 25: In a generative model for classification of classes $c_1, ..., c_r$, the probability of generating data point $d$ is
	- $Pr(d) = \sum_{i=1}^{r}{Pr(d | c_i)Pr(c_i)}$
- Question 26: What are the parameters of a multinomial distribution?
	- The probability of each outcome and the number of independent trials